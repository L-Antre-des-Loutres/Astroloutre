export type ServeurType = {
    nom : string,
    jeu : string,
    version: string,
    modpack: string,
    modpack_url: string,
    nom_monde: string,
    embed_color: string,
    actif: boolean,
    global: boolean
    image: string,
    description: string,
    contenaire: string,
    type: string
}
